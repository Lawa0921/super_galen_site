# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 專案概述

這是一個結合 **Jekyll 靜態網站** + **RPG 遊戲化介面** + **Web3/區塊鏈整合** 的全端開發者作品集部落格。

**核心特色**：
- 🎮 **RPG 風格互動介面**：完整的角色狀態系統、技能樹、物品欄、成就系統
- ⛓️ **區塊鏈整合**：可升級的 ERC-20 代幣智能合約（SuperGalenToken）與 DApp 介面
- 🌐 **完整 i18n 支援**：支援繁中、簡中、英、日、韓五種語言
- 📝 **技術部落格**：24+ 篇技術文章，涵蓋 Web3、前端、後端主題
- 🎨 **純手工打造**：無前端框架，純 Vanilla JS + Jekyll + SCSS

## 技術棧

### 前端
- **核心**：HTML5、CSS3/Sass、Vanilla JavaScript (ES6+)
- **樣式**：SCSS 模組化架構（30+ partials）
- **特效**：Canvas 技能樹、CSS 動畫、像素風格設計
- **多語系**：自製 i18n 系統（YAML → JSON → 動態載入）

### 後端 & 靜態網站生成
- **框架**：Jekyll 4.x（GitHub Pages 相容）
- **語言**：Ruby（Gemfile 管理依賴）
- **插件**：jekyll-feed、jekyll-seo-tag、jekyll-sitemap、jekyll-paginate

### 區塊鏈
- **智能合約**：Solidity（SuperGalenTokenV1 可升級 UUPS 模式）
- **開發框架**：Hardhat 2.26.x
- **Web3 函式庫**：ethers.js v6.15
- **升級模式**：OpenZeppelin UUPS Proxy
- **測試**：Hardhat + Chai（2 個測試檔案，76 個測試案例）
- **支援網路**：Localhost (31337)、Polygon (137)

### 開發工具
- **版本控制**：Git + GitHub
- **套件管理**：npm（前端/區塊鏈）、Bundler（Jekyll）
- **程式碼品質**：Solhint（Solidity Linter）

## 開發指令

### 快速啟動（推薦）

```bash
# 一鍵啟動完整開發環境（Hardhat + 合約部署 + Jekyll）
./dev
```

這個腳本會自動：
1. 啟動 Hardhat 本地節點（端口 8545）
2. 部署智能合約到本地網路
3. 生成前端合約配置（`assets/js/contracts-config.js`）
4. 啟動 Jekyll 開發伺服器（端口 4000）
5. 顯示測試帳戶與合約地址

按 `Ctrl+C` 可停止所有服務。

### Jekyll 開發

```bash
# 安裝 Ruby 依賴
bundle install

# 啟動開發伺服器（自動監控檔案變更）
bundle exec jekyll serve --host 0.0.0.0

# 編譯為生產環境
JEKYLL_ENV=production bundle exec jekyll build
```

**訪問地址**：http://localhost:4000

**重要**：
- Jekyll 會自動監控大部分檔案變更，但 **i18n JSON 檔案例外**
- 修改 `_data/i18n/*.yml` 後必須執行 `ruby generate_i18n_json.rb`
- 修改 `_config.yml` 需重啟伺服器

### 區塊鏈開發

```bash
# 編譯智能合約
npm run compile

# 執行所有測試（76 個測試案例）
npm test

# 執行特定測試檔案
npx hardhat test blockchain/test/SuperGalenTokenV1.test.js

# 啟動本地區塊鏈節點（Hardhat Network）
npm run node

# 部署到本地網路（需在另一個終端機先啟動節點）
npm run deploy:local

# 部署到 Polygon 主網（需要 .env 設定）
npm run deploy:polygon

# 執行 Solidity Linter
npx solhint 'blockchain/contracts/**/*.sol'
```

### i18n 工作流程

**流程**：
```
YAML (_data/i18n/*.yml) → Ruby 腳本 → JSON (assets/i18n/*.json) → JS 載入器 → DOM
```

**步驟**：
1. 編輯 `_data/i18n/{lang}.yml`（支援：zh-TW, zh-CN, en, ja, ko）
2. **必須執行**：`ruby generate_i18n_json.rb`
3. 重新整理瀏覽器查看變更

**使用方式**：
- **HTML 文字**：`<span data-i18n="key.path">預設文字</span>`
- **HTML 屬性**：`<div data-i18n-title="key.path" title="預設">...</div>`
- **JavaScript**：`I18nManager.t('key.path')` 或 `window.i18n.t('key.path')`

## 架構概覽

### 前端架構

**三層載入系統**：
1. **同步層**（`i18n-manager.js`）：在 DOM 之前載入，立即翻譯 `data-i18n` 屬性
2. **延遲層**（大部分腳本）：使用 `defer` 屬性進行非阻塞載入
3. **事件驅動層**：管理器透過 `Promise.all()` 等待依賴，而非輪詢

**核心管理器（單例模式）**：
- `GameStateManager`：透過 Cookie 持久化玩家狀態（HP/MP/SP/Gold）
- `I18nManager`：多語言支援，動態載入翻譯 key
- `UnifiedWalletManager`：Web3 錢包連接（純 ethers.js 實作）
- `SGTPurchaseManager`：使用 USDT 購買代幣的流程
- `NetworkMonitor`：Chain ID 偵測與網路切換
- `SimpleSGTBalance`：以事件驅動方式顯示錢包 SGT 餘額

**腳本載入順序（依賴關係關鍵）**：
```
1. i18n-manager.js（同步載入，必須最先）
2. gamestate.js（defer）
3. security-utils.js（defer）
4. 所有其他管理器（defer，可並行）
5. main.js（defer，協調所有初始化）
```

**JavaScript 模組（25 個檔案）**：
- `main.js`：主協調器，初始化所有系統
- `gamestate.js`：遊戲狀態管理（HP/MP/SP/Gold/死亡復活）
- `i18n-manager.js`：多語系系統核心
- `skill-tree-hierarchical.js`：Canvas 技能樹渲染
- `inventory.js`：Diablo 2 風格物品欄系統
- `achievements.js`：成就系統
- `book.js`：背景故事書系統
- `sgt-purchase-manager.js`：代幣購買 UI
- `unified-wallet-manager.js`：Web3 錢包整合
- `simple-network-monitor.js`：網路狀態監控
- `simple-sgt-balance.js`：代幣餘額顯示
- 其他：`cheat.js`、`cyber-terminal.js`、`party.js`、`summon.js` 等

### 區塊鏈架構

**智能合約**：
- `SuperGalenTokenV1.sol`：可升級 ERC20 代幣（UUPS 模式）
  - **購買機制**：以可配置比例將 USDT 兌換為 SGT
  - **安全功能**：
    - 時間鎖（Treasury 24h、MintRatio 1h、MaxSupply 7d）
    - 基於角色的訪問控制（ADMIN_ROLE、MINTER_ROLE、PAUSER_ROLE）
    - 黑名單功能
    - 最大供應量限制
  - **升級機制**：UUPS Proxy Pattern（可升級合約邏輯）

- `MockUSDT.sol`：本地開發用的 USDT 模擬合約

**部署配置**：
- **Localhost (31337)**：
  - 20 個測試帳戶，每個 10,000 ETH
  - 使用 MockUSDT
  - 快速迭代開發

- **Polygon (137)**：
  - 真實 USDT：`0xc2132D05D31c914a87C6611C10748AEb04B58e8F`
  - 需要 `.env` 設定：`PRIVATE_KEY`、`POLYGON_RPC_URL`

- **合約地址儲存**：
  - 後端：`blockchain/deployments/deployed_addresses.json`
  - 前端：`assets/js/contracts-config.js`（自動生成）

**測試覆蓋（76 個測試）**：
- 代幣鑄造/銷毀與安全限制
- USDT 購買流程與邊界情況
- 基於角色的訪問控制
- 時間鎖機制（Treasury、MintRatio、MaxSupply）
- 黑名單功能
- 溢位/下溢保護
- 升級功能測試

### RPG 遊戲系統

**持久化狀態（基於 Cookie）**：
- HP/MP/SP 能量條與最大值
- 金幣貨幣系統
- 死亡/復活機制（60 秒倒數，復活時 HP/SP 為 10%）
- 最後訪問時間戳
- 跨頁面狀態同步

**8 個主要互動頁籤**：
1. **狀態**：屬性點數（STR/INT/BUG/SAN/STS/CAF/OVT/DLN/LUK）與增減益效果
2. **技能**：Canvas 技能樹（5 大分支：前端/後端/DevOps/區塊鏈/生活技能）
3. **故事**：個人簡介與職涯時間軸
4. **物品**：Diablo 2 風格裝備欄與背包系統
5. **成就**：14 個成就（武林高手、日語達人、轉職成功者等）
6. **夥伴**：團隊成員展示
7. **購買**：SGT 代幣購買介面（USDT → SGT）
8. **日誌**：部落格文章列表（24+ 篇技術文章）

**技能樹系統**：
- 階層式結構：主分支 → 子分支 → 葉子節點
- 5 大主分支：
  - **前端技術**（藍色）：HTML/CSS/JS、Tailwind、Bootstrap、Stimulus、Hotwire、LiveView
  - **後端技術**（綠色）：Ruby/Rails/Sidekiq、Elixir/Phoenix、Node.js、PostgreSQL、MySQL
  - **DevOps**（橘色）：Git/GitHub、AWS、Terraform、Ansible、GitHub Actions
  - **區塊鏈**（紫色）：Solidity、ethers.js、Hardhat、IPFS、Token/DAO/DEX
  - **生活技能**（紅色）：桌遊、露營、客服、水電、問題解決、團隊合作

## 檔案結構

```
my-portfolio-blog/
├── dev                          # 一鍵啟動完整開發環境腳本
├── _config.yml                  # Jekyll 配置
├── _layouts/
│   └── default.html             # 主版面配置（包含腳本載入順序）
├── _data/
│   └── i18n/                    # 翻譯 YAML 檔案（zh-TW, zh-CN, en, ja, ko）
├── _posts/                      # 部落格文章（24+ 篇 Markdown）
├── assets/
│   ├── css/
│   │   ├── main.scss            # SCSS 入口點
│   │   └── _*.scss              # 30+ SCSS 模組檔案
│   ├── js/
│   │   ├── main.js              # 主協調器
│   │   ├── i18n-manager.js      # 多語系核心（必須同步載入）
│   │   ├── gamestate.js         # 遊戲狀態管理
│   │   ├── skill-tree-hierarchical.js  # Canvas 技能樹
│   │   ├── inventory.js         # 物品欄系統
│   │   ├── unified-wallet-manager.js   # Web3 錢包整合
│   │   ├── sgt-purchase-manager.js     # 代幣購買
│   │   └── contracts-config.js  # 合約配置（自動生成）
│   ├── i18n/*.json              # 生成的翻譯檔案（請勿直接編輯）
│   └── images/                  # 圖片資源
├── blockchain/
│   ├── contracts/
│   │   ├── SuperGalenTokenV1.sol   # 主代幣合約
│   │   └── MockUSDT.sol            # 測試用 USDT
│   ├── scripts/
│   │   ├── deploy-local-complete.js    # 本地部署腳本
│   │   └── deploy-supergalen-token.js  # Polygon 部署腳本
│   ├── test/
│   │   ├── SuperGalenTokenV1.test.js   # 合約測試
│   │   └── SuperGalenTokenV1Upgrade.test.js  # 升級測試
│   └── deployments/
│       └── deployed_addresses.json     # 部署記錄
├── generate_i18n_json.rb        # YAML→JSON 轉換器（必須手動執行）
├── hardhat.config.js            # 區塊鏈配置
├── package.json                 # npm 依賴
├── Gemfile                      # Ruby 依賴
└── index.html                   # 首頁（單頁應用）
```

## 關鍵注意事項

### 效能優化
- **消除渲染阻塞**：除了 `i18n-manager.js` 外，所有腳本都使用 `defer`
- **無輪詢機制**：透過 `Promise.all()` 進行事件驅動的依賴載入
- **Font Awesome 技巧**：使用 `media="print" onload="this.media='all'"` 實現非同步 CSS 載入
- **SCSS 編譯**：使用 `style: compressed` 壓縮 CSS

### 安全性
- **絕不提交 `.env`**：包含私鑰，已在 `.gitignore`
- **智能合約安全**：
  - 使用 SafeERC20 進行代幣轉帳
  - Treasury 變更需要 24 小時時間鎖
  - MintRatio 變更限制為 20%，冷卻期 1 小時
  - MaxSupply 變更需要 7 天冷卻期
  - 完整的 AccessControl 與 Pausable 機制
- **前端安全**：
  - 使用 `security-utils.js` 進行輸入驗證
  - XSS 防護：使用 `textContent` 而非 `innerHTML`
  - CSRF 防護：合約交易需要用戶簽名

### Web3 整合
- **純 ethers.js 實作**：無 WalletConnect SDK 臃腫
- **支援錢包**：MetaMask 和瀏覽器注入的錢包
- **自動網路偵測**：Chain ID 檢查與切換提示
- **合約配置**：動態載入失敗時有預設回退值
- **錯誤處理**：用戶拒絕交易、Gas 不足、網路錯誤等

### CSS 架構
- **SCSS 模組化**：30+ partials，在 `main.scss` 中匯入
- **絕不使用 `!important`**：除非絕對必要
- **禁止行內樣式**：所有樣式都在 SCSS 中定義
- **響應式斷點**：
  - Desktop: > 1024px
  - Tablet: 768px - 1024px
  - Mobile: < 768px
- **RPG 主題色**：
  - 金色：`#ffd700`（主色調）
  - 深藍漸層背景：`#0a0e27` → `#1a1f3a`
  - 分類色：藍（前端）、綠（後端）、橘（DevOps）、紫（區塊鏈）、紅（生活）

### i18n 工作流程
1. **永遠先編輯 YAML**：`_data/i18n/*.yml`
2. **必須執行轉換**：`ruby generate_i18n_json.rb`
3. **重新整理瀏覽器**：Jekyll 不會自動重載 JSON
4. **新增語言**：需同步更新 5 個檔案（zh-TW, zh-CN, en, ja, ko）
5. **測試多語系**：切換語言後檢查所有頁面

### 開發工作流程
1. **修改 YAML 翻譯**：
   - 編輯 `_data/i18n/*.yml`
   - **必須執行** `ruby generate_i18n_json.rb`
   - 手動重新整理瀏覽器

2. **修改智能合約**：
   - 編輯 `blockchain/contracts/*.sol`
   - 執行 `npm run compile`
   - 執行 `npm test` 確保所有測試通過
   - 重新部署：`npm run deploy:local`

3. **修改前端邏輯**：
   - 編輯 `assets/js/*.js`
   - Jekyll 會自動重載（但 `contracts-config.js` 除外）
   - 檢查瀏覽器 Console 是否有錯誤

4. **修改樣式**：
   - 編輯 `assets/css/_*.scss`
   - Jekyll 會自動重新編譯
   - 強制重新整理瀏覽器（Ctrl+Shift+R）

5. **新增部落格文章**：
   - 在 `_posts/` 新增 `YYYY-MM-DD-title.md`
   - Front Matter 必須包含 `layout`, `title`, `date`, `categories`
   - 使用 `future: true` 可顯示未來日期文章

### 測試策略
- **智能合約**：必須達到 100% 測試覆蓋
- **前端**：目前無自動化測試（未來可加入 Jest）
- **手動測試清單**：
  - [ ] 所有 8 個頁籤正常運作
  - [ ] 5 種語言切換正確
  - [ ] 錢包連接與斷開
  - [ ] 代幣購買流程
  - [ ] 響應式設計（Desktop/Tablet/Mobile）
  - [ ] 跨瀏覽器測試（Chrome/Firefox/Safari）

### Git 工作流程
- **提交訊息格式**：Conventional Commits
  - `feat(scope): 新功能描述`
  - `fix(scope): 修復描述`
  - `docs(scope): 文件更新`
  - `refactor(scope): 重構描述`
  - `chore(scope): 雜項變更`
- **末尾署名**：
  ```
  🤖 Generated with [Claude Code](https://claude.com/claude-code)

  Co-Authored-By: Claude <noreply@anthropic.com>
  ```
- **分支策略**：
  - `master`：主分支（可直接 commit）
  - 重大功能：建議使用 feature branch

### 常見陷阱與解決方案

**問題 1：i18n 翻譯沒更新**
- ❌ 錯誤：直接編輯 `assets/i18n/*.json`
- ✅ 正確：編輯 `_data/i18n/*.yml` → 執行 `ruby generate_i18n_json.rb`

**問題 2：合約部署後前端連不上**
- ❌ 錯誤：忘記重新生成 `contracts-config.js`
- ✅ 正確：使用 `./dev` 腳本，或手動執行部署腳本（會自動生成配置）

**問題 3：Jekyll 沒有重載變更**
- ❌ 錯誤：期待所有檔案都自動重載
- ✅ 正確：修改 `_config.yml` 需重啟伺服器、JSON 需手動重新整理

**問題 4：技能樹顯示空白**
- ❌ 錯誤：i18n 未載入完成就初始化技能樹
- ✅ 正確：技能樹會監聽 `i18nInitialized` 事件，確保順序正確

**問題 5：合約測試失敗**
- ❌ 錯誤：修改合約後沒重新編譯
- ✅ 正確：先執行 `npm run compile` 再執行 `npm test`

**問題 6：部署到 Polygon 失敗**
- ❌ 錯誤：`.env` 檔案格式錯誤或缺少變數
- ✅ 正確：確認 `PRIVATE_KEY`（不含 0x）和 `POLYGON_RPC_URL` 正確

### 效能檢查清單
- [ ] SCSS 使用 `compressed` 模式
- [ ] JavaScript 使用 `defer` 載入（除了 i18n-manager.js）
- [ ] Font Awesome 使用非同步載入
- [ ] 圖片使用 WebP 格式（fallback 到 PNG）
- [ ] Canvas 技能樹使用 requestAnimationFrame
- [ ] Cookie 狀態同步使用防抖（debounce）

### 部署檢查清單
- [ ] 所有智能合約測試通過（76/76）
- [ ] i18n 翻譯已生成（5 種語言）
- [ ] `_config.yml` 的 `url` 和 `baseurl` 正確
- [ ] 移除 `_config.yml` 的 `future: true`（生產環境）
- [ ] 檢查 `.gitignore` 排除敏感檔案
- [ ] 執行 `JEKYLL_ENV=production bundle exec jekyll build`
- [ ] 檢查 `_site/` 輸出是否正確

## 技術決策記錄

### 為什麼選擇 Jekyll？
- GitHub Pages 原生支援
- Ruby 生態系成熟
- 部落格功能完整
- 無需伺服器，純靜態網站

### 為什麼不用前端框架？
- 學習 Vanilla JS 基礎
- 減少依賴與打包複雜度
- 更好的 SEO（無需 SSR）
- 完全掌控載入順序

### 為什麼選擇 UUPS 而非 Transparent Proxy？
- Gas 成本更低
- 升級邏輯在實作合約中（更靈活）
- OpenZeppelin 推薦的新模式

### 為什麼使用 Cookie 而非 LocalStorage？
- 跨頁面同步更簡單
- 可設定過期時間
- 適合遊戲狀態持久化

### 為什麼自製 i18n 而不用 jekyll-polyglot？
- 完全掌控前端動態切換
- 支援 JavaScript 翻譯
- YAML → JSON 工作流程清晰
- 避免插件在 GitHub Pages 的限制

## 聯絡資訊

- **GitHub**: [@Lawa0921](https://github.com/Lawa0921)
- **網站**: https://lawa0921.github.io
- **Email**: bag571ivy3470@gmail.com
- **Discord**: https://discord.gg/QRyTagwSSF

---

**最後更新**：2025-01-17
**專案狀態**：🟢 活躍開發中
**部署狀態**：🌐 已部署至 GitHub Pages
